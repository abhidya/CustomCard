# Workstreams

This file is the orchestration map for the active production-readiness push. It
links each lane to the design source, defined task shape, current evidence, and
remaining implementation gaps. It is intentionally stricter than a roadmap: a
lane is not "done" until its graduation evidence exists in the repo.

## Coordination Snapshot

No worker branch is currently awaiting integration. Recent merged lanes:

| Lane | Owner | Main commit | Scope | Validation |
| --- | --- | --- | --- | --- |
| Raw invite/ICS import metadata | Lead agent | `a0e163d` | `/api/import-preview` now accepts explicit `metadataOnlyPayload` or server-parsed raw invite/ICS fields (`rawImportText`, `rawInviteText`, `rawIcsText`, `rawCalendarText`), derives title/recipient/start/timezone/evidence on the server, returns parser evidence, and keeps raw calendar descriptions out of persisted records and API responses. | Focused parser/API/infra tests, API doctors, Postgres runtime doctor, full `npm run check`, diff whitespace check. |
| Coupon print-link browser evidence Module | Lead agent | `d70b38b` | Rendered Walgreens/CVS print-link evidence parsing, validation, and proof-status classification now live in `src/printerCouponBrowserEvidence.ts`; the operator collector imports that tested Module, records validation errors, refreshes the exact print-link evidence artifact from a no-login/no-upload/no-order Chrome run, and still blocks coupon best-price ranking until same-cart provider-portal subtotal proof exists. | Full `npm run check`, focused browser-evidence/pricing/provider/portal tests, live rendered coupon collector, attached-evidence collector, pricing doctor, diff whitespace check. |
| Coupon portal evidence import | Lead agent | `be346c9` | The coupon collector can now import an operator-only `CUSTOMCARD_COUPON_PORTAL_EVIDENCE` artifact, validate same-cart provider-portal coupon proof against source price observation, subtotal math, quantity, fulfillment mode, account state, coupon validity window, review timestamp, and no-order blocked fields, then hydrate matching offers so best-price ranking can use only accepted portal-applied evidence. Rejected, malformed, expired, future-dated, unsafe, or mismatched records are reported without changing prices. | Focused portal/pricing/provider tests, TypeScript no-emit check, pricing doctor, no-proof collector run, valid temporary portal-evidence collector run, script syntax check, reviewer timestamp finding fixed, diff whitespace check. |
| Coupon provider collector seam | Lead agent | `a618d88` | FMTC and Rakuten coupon-provider feed request builders, parsers, token redaction, credential-gated no-token behavior, and Walgreens/CVS deal filtering now live in `src/printerCouponProviderFeeds.ts` with focused tests; the operator collector imports that seam while preserving no-client-network, no-checkout, no-order, and no-discount-without-provider-portal-proof safeguards. Rakuten uses the current `api.linksynergy.com` coupon-feed endpoint. | Full `npm run check`, focused provider/pricing tests, TypeScript no-emit check, pricing doctor, no-credential coupon collector run, script syntax check, reviewer agent endpoint correction, diff whitespace check. |
| Mobile browser customer snapshot | Lead agent | `cb67513` | The direct mobile browser route now renders the shared `mobileRenderSnapshot` used by the Expo shell, removes the visible contract/debug panel, replaces raw array rendering with customer-facing snapshot rows, exposes one visible primary action, preserves checkout safeguards, and keeps the route from rendering JSON, Expo manifest, or bundle metadata. | Full `npm run check`, focused app-smoke/mobile/pricing tests, TypeScript no-emit check, Chrome CDP desktop/mobile screenshot review, diff whitespace check. |
| Coupon checkout code visibility | Lead agent | `81d50ff` | Customer print options now show the active source-listed Walgreens/CVS coupon code for the selected print shop while keeping the estimate unchanged until same-cart provider-portal evidence proves the discount; the rendered Walgreens/CVS print-link evidence artifact was refreshed from a no-login/no-upload/no-order Chrome collector run. | Full `npm run check`, focused pricing/UI smoke tests, pricing doctor, live rendered coupon collector, attached-evidence coupon collector, Chrome CDP desktop/mobile screenshots, diff whitespace check. |
| API mutation required fields | Lead agent | `4615f16` | Repository-backed non-import mutations now fail closed before idempotency/repository writes when required customer/order/privacy inputs are missing: render packets require `projectId`, card projects require opportunity and recipient, memory review requires recipient/text/decision, manual print handoff requires project/render/store/approval, and data requests require request type, region, and customer confirmation; stale placeholder defaults were removed from those builders. | Full `npm run check`, focused API/infra tests, Postgres runtime doctor, diff whitespace check. |
| Import-preview metadata guard | Lead agent | `9914455` | `/api/import-preview` now rejects missing `sourceKind`, title, recipient, or start time metadata before idempotency/repository persistence; contract and memory API tests cover the 400 response, the Postgres runtime doctor proves rejected requests do not create records, and stale placeholder defaults were removed from the import-preview builders. | Full `npm run check`, focused API/infra tests, Postgres runtime doctor, diff whitespace check. |
| Runtime import source guard | Lead agent | `01121c1` | Provider-runtime local import/export adapters now block blank source text instead of substituting bundled samples, while explicit fixture inputs remain only for intentional catalog dry-run coverage; docs and the business engagement doctor record the missing-source contract. | Full `npm run check`, focused provider runtime tests, business engagement doctor, diff whitespace check. |
| Coupon source collection status | Lead agent | `c6407ac` | Coupon collection output now records scraped offer activity plus explicit best-price blockers, refreshed the rendered Walgreens/CVS print-link evidence, documents that CVS `GRADUATION` navigation text is not coupon proof, and keeps FMTC/Rakuten as credential-gated provider feeds while source-listed `CRISPCARD`/`JUNESW` still require same-cart provider-portal evidence before discounting. | Full `npm run check`, pricing doctor, coupon collector, rendered print-link collector, focused pricing tests, diff whitespace check. |
| Customer action hierarchy | Lead agent | `f7a2b47` | Customer web overview now renders exactly one prominent next-step CTA per setup/event/proof state, moves secondary tasks into compact supporting actions, retitles the customer print card away from internal fulfillment language, and guards customer snapshots plus local chat copy against internal readiness terms like handoff/provider jargon. | Full `npm run check`, focused customer web/chat/browser smoke tests, build, Chrome CDP desktop/mobile screenshot review, diff whitespace check. |
| Retail certification packet contracts | Lead agent | `e980196` | Retail printer provider identity, exact Walmart/FedEx/CVS/Walgreens links, operation blueprints, URL validation, and certification evidence packets now live in `src/retailPrinterContracts.ts`; the adapter module consumes that deeper contract seam to emit blocked no-network price/upload/order packets with `certificationPacket`, `canEnableLiveOperation: false`, provider evidence fields, and live-enablement checks. | Full `npm run check`, focused retail/provider/pricing tests, retail doctor, build, diff whitespace check. |
| Local runtime profile vocabulary | Lead agent | `58aecd0` | Current architecture profile ids now use the existing `local-dev` vocabulary instead of `prototype`, the local profile is titled as a development contract, docs distinguish current local architecture from rejected prototype history, and domain tests reject the old runtime literal while keeping intentional mockup-artifact and admin demo/reset safety terms untouched. | Full `npm run check`, focused domain/infra tests, build, diff whitespace check. |
| Fulfillment contract-review mode | Lead agent | `1f004e8` | Fulfillment adapter output now uses `contractReview` instead of `mock`, agent contract tests reject the old literal, R019 traceability describes deterministic dry-run testing, and domain architecture copy names certification-gated retail contracts without changing print mockup-artifact safety checks. | Full `npm run check`, focused agent/domain/infra tests, diff whitespace check. |
| Reviewer bootstrap naming cleanup | Lead agent | `2f758dd` | Customer app bootstrap defaults moved from demo-named symbols into `src/reviewerBootstrap.ts`, the React shell now imports reviewer/local bootstrap contracts, coverage instrumentation and tests guard against `demoBootstrap` regression, and admin-only demo reset contracts remain intentionally unchanged. | Full `npm run check`, focused reviewer bootstrap/app-smoke/infra/free-MVP tests, build, diff whitespace check. |
| Mobile browser demo route | Lead agent | `bb0ee7d` | Mobile app preview now has a direct Vite browser route at `/?view=mobile`, a `npm run mobile:web:demo` command, URL-synced navigation, anti-JSON-manifest smoke coverage, docs that distinguish browser UI review from Expo metadata, and updated R004 traceability without claiming emulator or signed-artifact proof. | Full `npm run check`, focused app-smoke/mobile/infra tests, mobile shell doctor, mobile render doctor, mobile release doctor, build, Browser DOM check at 390px, diff whitespace check. |
| Coupon provider feed priority | Lead agent | `41d09b2` | Coupon collection now models FMTC and Rakuten as credential-gated provider feeds, keeps official Walgreens/CVS pages and exact rendered print links as fallback evidence, emits provider-source priority in collector output, refreshes browser evidence, and still requires same-cart provider-portal evidence before best-price discounting. | Full `npm run check`, printer pricing doctor, coupon collector, rendered coupon collector, focused pricing/API contract tests, diff whitespace check. |
| Mobile customer copy guard | Lead agent | `fd2b271` | Mobile contract, Expo preview wrapper, doctors, and smoke tests now use customer-facing labels for the mobile jobs, expose `collectMobileCustomerCopy`, and block customer-visible implementation-stage terms such as OAuth-gated, handoff, fulfillment, adapter, mock, dummy, and placeholder. | Full `npm run check`, mobile render doctor, mobile release doctor, mobile shell doctor, focused mobile/infra/app-smoke tests, diff whitespace check. |
| Provider product URL adapters | Lead agent | `f92b2b1` | Walmart, FedEx, CVS, and Walgreens retail printer product links are now canonical in `retailPrinterProductLinks`; provider catalog docs URLs derive from the Adapter Module, generic/placeholder product links are rejected, the print page shows one selected print-shop link, and customer copy now favors print-ready files, price estimate, and discount confirmation language. | Full `npm run check`, retail doctor, pricing doctor, focused retail/provider/customer/app-smoke tests, diff whitespace check. |
| Coupon portal application packets | Lead agent | `dcb8f91` | Coupon pricing now emits per-offer provider-portal application packets with same-cart targets, expected discount math, operator steps, no-order blocked fields, collector output, API counts, and customer `Coupon checks` visibility while still preventing best-price discounting without matching portal evidence. | Full `npm run check`, pricing doctor, coupon collector, focused pricing/API/app-smoke tests, script syntax checks, diff whitespace check. |
| Mobile calendar parity | Lead agent | `f1cb952` | Mobile customer contract now shows concrete calendar choices: paste invite/ICS ready now, Google OAuth-readiness gated with data/credential boundaries, and Apple manual ICS export, with stale future-sync copy blocked by tests. | Full `npm run check`, mobile contract/render tests, mobile doctor, render doctor, release doctor, app smoke, diff whitespace check. |
| Calendar onboarding packets | Lead agent | `a708beb` | Calendar onboarding now exposes explicit local/Google/Apple action packets with customer steps, operator evidence, official source anchors, no-request/no-credential boundaries, and fail-fast readiness contract lookup instead of silent fallback defaults. | Full `npm run check`, focused onboarding contract tests, build, audit, diff whitespace check. |
| Retail operation packets | Lead agent | `b8d0deb` | Walmart, FedEx, CVS, and Walgreens operation Adapters now return no-network `operationPacket` evidence checklists for price, image upload, and order attempts, with source links, operator steps, safety checks, source-backed fields, and missing-input tracking while keeping request preparation blocked. | Retail doctor, pricing doctor, focused retail/runtime/infra tests, build, diff whitespace check. |
| Customer print copy cleanup | Lead agent | `898e2ab` | Customer web, mobile, API bootstrap, and smoke-copy tests now use plain customer language around cards, proof review, checkout status, and print options instead of MVP, handoff, vendor, OAuth, production-safety, or coupon-evidence terms. | Full `npm run check`, focused customer/API/mobile/infra suite, security doctor, e2e coverage doctor, printer pricing doctor, diff whitespace check. |
| Rendered print-link coupon collector | Lead agent | `54f469c` | Coupon collector can now open exact Walgreens/CVS print links with local Chrome, persist browser evidence, attach that proof to coupon collection output, and still block best-price discounting without same-cart provider-portal evidence. | Full `npm run check`, pricing doctor, collector with persisted rendered evidence, collector with attached evidence artifact, script syntax check, build, diff whitespace check. |
| Coupon render proof distinction | Lead agent | `ba90057` | Coupon collection now separates official page scrape evidence, static HTML print-link signals, optional browser proof artifacts, and same-cart provider portal evidence. CVS print-link browser evidence is attached as visible-code proof; Walgreens is recorded as product/price visible with coupon code in page HTML only, so visible proof remains required. | Focused pricing tests, pricing doctor, coupon collector with browser evidence artifact, build, diff whitespace check. |
| Retail adapter operation Interface | Lead agent | `926fb5d` | Walmart, FedEx, CVS, and Walgreens retail adapters now persist operation-specific product/price/upload/order source links and expose callable no-network methods for price, image upload, and order attempts without preparing requests. | Full `npm run check`, retail doctor, focused retail/runtime tests, build, diff whitespace check. |
| Provider coupon proof | Lead agent | `10d83f2` | Coupon collector now extracts codes from exact Walgreens/CVS print links, reports print-link code/price evidence, supports credential-gated FMTC provider-feed polling with token redaction, and keeps discounts out of best-price ranking without same-cart portal proof. | Full `npm run check`, pricing doctor, coupon collector, focused pricing tests, build, diff whitespace check. |
| Admin operations workflow | Lead agent | `8f9dc82` | Admin panel now exposes a tested integration-owner workflow for credential vault, hosted token proof, alert drill, incident runbook, and owner-lane evidence actions. | Full `npm run check`, admin operations doctor, browser smoke suite, Browser desktop DOM/console check. |
| Customer web flow | Lead agent | `52efae6` | Customer dashboard now uses a tested one-primary-action journey model that routes event review before drafting and keeps customer copy free of internal provider/model jargon. | Full `npm run check`, browser smoke suite, focused customer/chat/API/provider/pricing tests, Browser desktop DOM/console check. |
| Mobile render snapshot | Lead agent | `e9b6580` | Expo mobile shell renders a tested customer-facing snapshot instead of importing raw contract arrays. | Full `npm run check`, mobile doctor, mobile render doctor, release doctor, focused mobile/infra tests. |
| Coupon source targets | Lead agent | `3c7543d` | Official Walgreens/CVS coupon sources, exact print-link code/price proof targets, and credential-gated FMTC provider-feed reporting. | Full `npm run check`, pricing doctor, coupon collector, focused pricing/API/app tests. |
| Retail operation blueprints | Lead agent | `e1d74d2` | Retail printer operations now carry blocked request/evidence blueprints for future certified price fetch, image upload, and order placement. | Full `npm run check`, retail doctor, focused retail/runtime tests. |
| Coupon portal evidence | Anscombe | `6ffd44d` | Structured same-cart provider-portal coupon evidence before best-price coupon ranking. | Full `npm run check`, pricing doctor, script syntax checks, focused pricing/API/fulfillment tests. |
| Demo/bootstrap boundary | Huygens | `dd71043` | Demo bootstrap values moved out of the primary React shell without behavior change. | Browser smoke tests and build. |
| Orchestration/workstream map | Lead agent | `c14c401` | Repo-local workstream map, branch hygiene, integration, and validation sequencing. | Infra contract test and diff whitespace check. |

## Product Surface Workstreams

| Workstream | Design source | Tasks already defined | Current coverage | Implementation gaps |
| --- | --- | --- | --- | --- |
| Customer web app | `docs/product-brief.md`, `docs/implementation-roadmap.md`, `docs/requirements-traceability.md` R021 | Local workspace, invite/ICS import, opportunity review before drafting, card proof path, memory controls, local chat, review-only fulfillment recommendations. | Vite/React app, tested customer web experience view-model, reviewer/local bootstrap module, one-primary-action customer journey, browser smoke coverage, runtime console-error guard, local deterministic workflows. | Hosted production auth, live account recovery UX, real provider import, real payment/order flow, accessibility audit evidence. |
| Admin website | `docs/platform-expansion-design.md`, `docs/requirements-traceability.md` R021-R029 | Provider inventory, readiness gates, production launch gates, external audit/payment/mobile/hosted/cloud proof registers, owner-lane admin operations workflow. | Admin panel surfaces catalog, runtime, readiness, credential-vault action, hosted token proof, alert/incident drills, and blocked providers without enabling network calls. | Real credential-vault secret storage, production incident/alert evidence, hosted admin token proof output, integration-owner signoff workflow in a deployed admin account. |
| Native iOS/Android app | `apps/mobile/README.md`, `src/mobileRenderReadiness.ts`, `docs/requirements-traceability.md` R004 | Expo shell, API URL config, customer app contract, render snapshot, offline sync model, release profile doctor, concrete calendar import choices, customer-copy guard, direct browser mobile UI route. | Native source contract, tested render snapshot consumed by Expo root, mobile doctor, release doctor, `npm run mobile:web:demo` direct route, 390px browser mobile-preview smoke with anti-JSON-manifest assertions, mobile calendar data/credential boundaries, and customer-copy collector blocking implementation-stage jargon. | React Native renderer-package output, emulator screenshots, EAS build execution, signed iOS/Android artifacts, store-review evidence. |
| Onboarding and calendars | `docs/onboarding-calendar-plan.md`, `src/onboardingCalendar.ts`, R030 | User stories, staged onboarding, Google Calendar OAuth readiness, iCloud manual ICS export, ready `Paste invite or ICS` path, explicit action packets. | Typed contracts, customer-visible choices, source-backed action packets, fail-fast missing-contract lookup, server-side raw invite/ICS parsing for `/api/import-preview`, tests, docs, no fake Google/Apple CTAs. | Live OAuth app approvals, callback/token storage, revocation drill, hosted provider sync worker, metadata schema proof against real provider payloads beyond manual paste/ICS fixtures. |

## Provider And Fulfillment Workstreams

| Workstream | Design source | Tasks already defined | Current coverage | Implementation gaps |
| --- | --- | --- | --- | --- |
| Retail printer adapters | `src/retailPrinterAdapters.ts`, `src/providerCatalog.ts`, `docs/printer-pricing-research.md` | Walmart/FedEx/CVS/Walgreens adapter plans for price fetch, image upload, and order placement; manual fallback remains default. | Blocked adapter contracts with canonical exact product links, persisted product/price/upload/order source links, callable no-network operation adapters, operation request blueprints, placeholder/generic URL rejection, retail readiness register, selected print-shop customer link, and manual print package export. | Credentialed quote/upload/order APIs, vendor certification, kill-switch enforcement outside provider runtime, live quote snapshots, physical print QA. |
| Coupons and best price | `src/printerPricing.ts`, `src/fulfillmentRecommendation.ts`, `src/printerCouponBrowserEvidence.ts`, `docs/printer-pricing-research.md` | Official Walgreens/CVS page collection, exact Walgreens/CVS print-link code/price proof targets, FMTC and Rakuten provider-feed targets, provider-portal coupon application policy, same-cart application packets. | Review-only coupon offers and freshness rules; recommendations state coupons affect best price only after provider-portal application evidence; collector separates credentialed provider feeds, official coupon pages, static HTML print-link signals, validated browser proof artifacts, optional credentialed FMTC/Rakuten feed polling, and per-offer provider-portal application packets. | Same-cart provider portal proof attachment, tax/stock/pickup-window confirmation, provider-feed credentials, licensed feed coverage validation, legal review of any scraping path. |
| Payments | `src/paymentReadiness.ts`, `src/paymentReadinessData.mjs` | Sandbox payment request contracts, no-card-storage boundary, webhook/refund/live approval gates. | Stripe/PayPal/Square/Adyen sandbox/no-network readiness, payment doctor, no live charge/refund/capture claims. | Processor live-mode approval, PCI/legal review, webhook signature/replay proof, refund/void/dispute drills, settlement reconciliation. |
| AI/image providers | `src/aiProviderReadiness.ts`, `src/providerRuntime.ts`, `src/customerChat.ts` | Text/image provider inventory, prompt/privacy/spend/QA gates, deterministic local chat fallback. | Local deterministic chat, provider dry-run contracts, no live model calls. | Live model allowlist, provider credentials, print QA run, spend/abuse monitoring, moderation evidence, model-output provenance in production storage. |

## Infrastructure And Evidence Workstreams

| Workstream | Design source | Tasks already defined | Current coverage | Implementation gaps |
| --- | --- | --- | --- | --- |
| Hosted API and database | `src/hostedApiReadiness.ts`, `src/reviewerDbSeedReadiness.ts`, `docs/deployment-evidence.md` | Vercel deployment evidence, hosted API proof register, reviewer seed proof register, Postgres doctors. | Local/isolated live Postgres doctors, process-level HTTP doctor, Vercel config/evidence register. | Public DB-backed hosted route proof, hosted token verification, Vercel env sync, hosted reviewer seed execution, backup/restore evidence. |
| Cloud artifact storage | `src/cloudArtifactProofReadiness.ts`, `infra/aws/artifact-store`, `docs/deployment-evidence.md` | Terraform/IAM/static bucket contract, signed URL proof gates, object-store doctors. | Filesystem, injected S3-compatible, live MinIO/S3-compatible write/read verification, static AWS IaC contract. | Applied bucket ARN/IAM proof, real signed URL cloud probes, access-log proof, secret-manager sync, restore drill. |
| Observability and audits | `src/observabilityReadiness.ts`, `src/externalAuditReadiness.ts` | Telemetry schema, redaction, sampling, alert-route, incident-review, external audit register. | Repo-local readiness doctors and tests keep live ingestion disabled. | Live telemetry project, alert delivery proof, retention enforcement, incident drill, external security/privacy/accessibility/legal audits. |
| Capacity and deployment | `src/capacityPlan.ts`, `infra/`, `docs/implementation-roadmap.md` | Local-dev, cheap-droplet, cloud-native, SaaS-scale profiles; Docker/Kubernetes manifests. | Deployment and capacity doctors validate local scaffolding and CI wiring. | Real droplet/Kubernetes deployment, measured load/cost evidence, production secrets, rollback procedure proof. |

## Graduation Rules

1. A workstream can merge only from its own branch/worktree with a focused
   commit and passing validation named in the commit trailers.
2. Customer-facing UI must not expose internal proof terms unless the surface is
   explicitly admin/reviewer-oriented.
3. Coupons may be displayed as source-listed offers, but they may affect best
   price only after same-cart provider-portal application evidence exists.
4. Live OAuth, model calls, payment charges, vendor uploads/orders, and external
   messages stay disabled until a doctor records credential, consent,
   revocation, kill-switch, and audit evidence.
5. Native mobile readiness remains contract-only until emulator render proof and
   signed artifacts are attached.
6. Main is pushed after each clean merge; if a browser/runtime issue appears
   after tests pass, it gets a separate fix before the next lane.
